FROM node:18-alpine

WORKDIR /app

COPY package.json pnpm-lock.yaml* ./

RUN corepack enable && pnpm i

COPY . .

# Run the TypeScript ingestion daemon directly in production
CMD ["npx", "tsx", "src/lib/ingestion.ts"]
